export interface DataModel {
    letter: string;
    frequency: number;
  }
export interface MyDataModel{
  name: string;
  age: number;
}

export interface LineModel{
  mes: string;
  x: number;
  y: number;
  z: number;
}